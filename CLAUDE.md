# 프로젝트 규칙

이 프로젝트에서 작업할 때 항상 지켜야 하는 규칙입니다.

## 1. 파일 경로 하드코딩 금지
- 코드에 특정 파일 경로를 직접 쓰지 않는다.
- 사용자가 그때그때 업로드/지정하는 파일을 입력받아 사용하도록 작성한다 (예: 업로드된 파일, 커맨드라인 인자, 환경변수, 설정 파일 등으로 경로를 받는다).

## 2. 데이터 파일 위치 및 버전 관리 제외
- 엑셀(.xlsx, .xls)·CSV 파일은 앱 폴더 밖에 둔다 (코드/앱 폴더와 분리).
- `.gitignore`에 엑셀·CSV 파일을 등록하여 git 추적에서 제외한다.

## 3. API 키 관리
- API 키 등 민감한 값은 코드에 직접 넣지 않는다.
- `.env` 파일에서 읽어와 사용한다.
- `.env`는 `.gitignore`에 포함시켜 커밋되지 않도록 한다.

## 4. 언어
- 답변은 항상 한국어로 한다.
